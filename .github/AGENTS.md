# GitHub Directory Guidance

This file is `wiki-mod/distcc-ng/.github/AGENTS.md` — a pointer only, not a second governance source. It exists solely so tools that look for an `AGENTS.md` under `.github/` still find their way to the real one: `wiki-mod/distcc-ng/AGENTS.md` (this repository's root-level file, linked here as [`../AGENTS.md`](../AGENTS.md)). Read that file in full, not summarized or partially, before making any change under `wiki-mod/distcc-ng/.github/**` — it governs those changes too, and this file restates none of its content.
