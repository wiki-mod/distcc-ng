# CLAUDE.md

This file is `wiki-mod/distcc-ng/CLAUDE.md`. It exists only to point Claude Code at `wiki-mod/distcc-ng/AGENTS.md` — it carries no governance, architecture, or workflow content of its own, and restates none of `AGENTS.md`'s rule text (not even word-identical); `wiki-mod/distcc-ng/AGENTS.md` is the single source of truth for all of that (see that file's rule 65 for this policy and rule 68 for which copy of these files is binding).

**Mandatory at the start of every session/task in the `wiki-mod/distcc-ng` repository**: read `wiki-mod/distcc-ng/AGENTS.md` in full, out of `current_dev` — see that file's rules 83-85 for exactly what "in full" requires, why `current_dev` specifically, and what to do when delegating to a subagent. It is not auto-loaded into context the way `wiki-mod/distcc-ng/CLAUDE.md` is; you must actively fetch and read it yourself. `wiki-mod/distcc-ng/.github/AGENTS.md` is a separate pointer file for tools that only look under `.github/` — it redirects to `wiki-mod/distcc-ng/AGENTS.md` too and restates none of its content either; `wiki-mod/distcc-ng/AGENTS.md` itself is always the real source.
