#!/bin/sh
# What: creates the distcc system user/group
# Why: --home /nonexistent matches this fork's RPM/deb convention
# From: #398

addgroup -S distcc 2>/dev/null
adduser -S -D -H -h /nonexistent -s /sbin/nologin -G distcc -g distcc distcc 2>/dev/null

exit 0
